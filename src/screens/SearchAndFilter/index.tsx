import { StyleSheet, Text, View } from "react-native";
import React from "react";

const SearchAndFilter = () => {
  return (
    <View>
      <Text>SearchAndFilter</Text>
    </View>
  );
};

export default SearchAndFilter;

const styles = StyleSheet.create({});
