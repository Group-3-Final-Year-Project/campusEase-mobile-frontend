import { StyleSheet, Text, View } from "react-native";
import React from "react";

const MoreBookingInfo = () => {
  return (
    <View>
      <Text>MoreBookingInfo</Text>
    </View>
  );
};

export default MoreBookingInfo;

const styles = StyleSheet.create({});
